<h1 class="text-center my-4"><i class="fas fa-bookmark"></i> Add Bookmark : <%= @list.name %></h1>

<%= simple_form_for [@list, @bookmark], html: { class: 'd-flex flex-column align-items-center' } do |f| %>
  <div class="w-50 mb-3">
    <%= f.input :movie, as: :select, collection: Movie.all, prompt: 'Sélectionner un film', input_html: { class: 'form-control' } %>
    <%= f.input :comment, label: 'Commentaire', input_html: { class: 'form-control' } %>
  </div>

  <%= f.button :submit, 'Add Bookmark', class: 'btn btn-primary w-50' %>
  <%= link_to 'Retour', list_path(@list), class: 'btn btn-secondary w-50 mt-3' %>

<% end %>
